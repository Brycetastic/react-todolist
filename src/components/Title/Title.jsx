import React, { Fragment } from 'react';
// import PropTypes from 'prop-types';

export default function Title(props) {
    return (
        <Fragment>
            <h1>Bryce's List</h1>
            <p>Start your list</p>
        </Fragment>
    );
}
